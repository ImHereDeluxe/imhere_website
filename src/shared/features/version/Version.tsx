// src/components/Version.tsx

import './Version.css';

const Version = () => {
    return (
        <div className="version-label">
            v1.0.0
        </div>
    );
};

export default Version;
