version: "3.8"

services:
  frontend:
    build: .
    container_name: imhere-frontend
    restart: unless-stopped
    networks:
      - imhere-network
    expose:
      - "80"

networks:
  imhere-network:
    external: true
